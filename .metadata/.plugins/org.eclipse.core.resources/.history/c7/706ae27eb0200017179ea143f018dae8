package com.wbl.page;

import java.util.ArrayList;
import java.util.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

public class AmazonCreateAccountPage {

	WebDriver driver;

	public AmazonCreateAccountPage(WebDriver driver) {

		this.driver = driver;
	}
	
	
	
	 

  	public String enterDetails(List<String> accountDetails) {
	
 //	public String enterDetails(String nm, String user, String pwd, String rePwd) {
 		
	
 
	
 	System.out.println("list details : " + accountDetails.get(0) + accountDetails.get(1));

 //	 	System.out.println("list details : " + nm + "  " + user + "  " + pwd + "  " + pwd2);
 		
 		
 		
 		
		
		WebDriverWait wait = new WebDriverWait(driver, 30);
		
		
	/*	  WebElement element = wait.until(ExpectedConditions.visibilityOfElementLocated(By.xpath(".//a[@id='createAccountSubmit']"))) ;
		
 		element.click();
		*/

		List<WebElement> account = new ArrayList<WebElement>();

		wait = new WebDriverWait(driver, 30);
 
 	account = wait
				.until(ExpectedConditions.visibilityOfAllElementsLocatedBy(By.cssSelector("[class*='a-input-text']")));
		 
		System.out.println("size of account : " + account.size());
 
		 
		/*  wait
		.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector("#ap_email"))).sendKeys(accountDetails.get(0));
		  
		  wait
			.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector("#ap_email"))).sendKeys(accountDetails.get(0));
		  
		  wait
			.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector("#ap_email"))).sendKeys(accountDetails.get(0));
		  
		  wait
			.until(ExpectedConditions.visibilityOfElementLocated(By.cssSelector("#ap_email"))).sendKeys(accountDetails.get(0));
		*/
 
		int i = 0;
		
		for (WebElement ele : account) {

  		ele.clear();
  		ele.sendKeys(accountDetails.get(i));
  		
  		 i++;
		} 
 
	/*	
		  account.get(0).sendKeys(nm);  
		  account.get(1).sendKeys(user);  
		  account.get(2).sendKeys(pwd);  
 		  
*/
		
		
		return "pallavi";	
		
	}

	// .//div[@class='a-alert-content'][contains(text(),'Passwords must match')]
	// // wrong renetry of password
 	
 	
 	
}
